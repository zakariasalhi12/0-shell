use crate::{Commande, parse};

// pub fn Execute(commandes: Vec<Commande>) {
//     for command in commandes {
//         let com = command.Name.execute(&command.Args);
//         com.execute();
//     }
// }
